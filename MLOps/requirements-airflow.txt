# Dependências que as TASKS da DAG precisam para rodar o pipeline dentro do
# container do Airflow. Deliberadamente enxuto (sem fastapi/uvicorn/optuna),
# para minimizar conflitos com as dependências fixadas pelo Airflow.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3
matplotlib>=3.7

# A task load_feature_store grava no PostgreSQL. O Airflow ja traz o
# SQLAlchemy, mas o driver psycopg2 e declarado aqui de proposito: e uma
# dependencia DA TASK, nao do orquestrador.
psycopg2-binary>=2.9
