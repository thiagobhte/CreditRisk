# ===== Sistema / editor =====
/.DS_Store
/.vscode
.idea/

# ===== Python =====
__pycache__/
*.pyc
venv/
.venv/
.ipynb_checkpoints/

# ===== Dados (grandes / regeneráveis) — não versionar =====
# A pasta /Dados existe no repositório (via Dados/README.md), mas os CSVs
# em si são grandes demais para o git e ficam ignorados.
/Dados/raw_data
/Dados/*.csv
/mnt

# ===== Saídas geradas na raiz =====
# As importancias moram em Model/artifacts/ e SAO versionadas, junto do
# .joblib que elas descrevem: o monitoring.py le esse CSV para saber quais
# variaveis acompanhar, entao um clone sem ele monitora 8 em vez de 30.
# (as duas linhas abaixo cobriam o caminho antigo, na raiz)
/submission_baseline.csv
/*.csv
drift_report.json

# ===== Documentos auxiliares de trabalho (não fazem parte da entrega) =====
# Guias internos de apoio — checklist, passo a passo, contexto e handoff.
# Ficam locais, não vão para o repositório.
/CHECKLIST_ENTREGA.md
/PASSO_A_PASSO_SERVICOS.md
/CONTEXTO_APRESENTACAO.md
/HANDOFF_CONTEXTO.md
/HANDOFF_RECUPERACAO.md
/ROTEIRO_APRESENTACAO.md

# Enunciado do trabalho (especificação da atividade, não é entregável)
/ProjetoFinal_v2.pdf

# Anotações pessoais de trabalho (repositório é público)
/perguntas professora.txt

# Observação: Model/artifacts/ (modelo treinado) É versionado de propósito,
# para que a API/predict funcionem sem precisar re-treinar. São ~4 MB.

# ===== Wheels para build offline (249 MB - nunca versionar) =====
wheelhouse/
